#!/usr/bin/env bash
# This script lists the contents of the current directory showing only the part after the first dash

for file in *; do
    # Skip hidden files
    [ -f "$file" ] || continue
    # Print part after the first dash
    echo "${file#*-}"
done
